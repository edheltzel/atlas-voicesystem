# macOS
.DS_Store

# Logs / temp
*.log
/tmp/

# Local agent scratch / draft issues (promoted to GitHub Issues, not committed)
.scratch/

# Project-local Atlas agent artifacts (plans, handoffs, worktrees) — not committed
.agents/atlas/

# Audio (runtime artifacts)
*.mp3
*.wav
*.aiff

# Editor / OS
.idea/
.vscode/
*.swp
