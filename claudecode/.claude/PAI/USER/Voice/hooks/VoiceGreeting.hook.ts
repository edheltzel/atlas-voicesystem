#!/usr/bin/env bun
/** Compatibility wrapper; implementation moved to adapters/pai/hooks/. */
await import(new URL("../../../../../../adapters/pai/hooks/VoiceGreeting.hook.ts", import.meta.url).href);
